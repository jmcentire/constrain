{
  "component_id": "posture",
  "contract_version": 1,
  "test_cases": [
    {
      "id": "goodhart_format_model_omits_default_fields",
      "description": "The format function should omit fields that are at their default (empty) values, not just produce a static string regardless of input",
      "function": "_format_problem_model",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Two ProblemModel instances each with a single different field populated",
      "expected_output_description": "Each output contains only the label and value for the one populated field, not labels for empty fields",
      "expected_error": "",
      "assertions": [
        "A model with only system_description set should NOT contain labels for dependencies, boundaries, assumptions, etc.",
        "The output for a model with only primary_goal set should differ from one with only stakes set"
      ]
    },
    {
      "id": "goodhart_format_model_list_bullets",
      "description": "List fields in ProblemModel should be rendered as bulleted sub-items, not just comma-separated or raw list repr",
      "function": "_format_problem_model",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with dependencies=['Redis cache', 'PostgreSQL 15', 'RabbitMQ']",
      "expected_output_description": "Each dependency rendered as a bulleted sub-item",
      "expected_error": "",
      "assertions": [
        "Output for a model with multiple dependencies contains bullet characters or dash-prefixed lines for each item",
        "Each list item appears on its own line or with its own bullet marker"
      ]
    },
    {
      "id": "goodhart_format_model_novel_content_passthrough",
      "description": "The format function must actually serialize the field values from the model, not return a pre-baked string",
      "function": "_format_problem_model",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with system_description='xK9mQ_unique_marker_alpha' and primary_goal='zW3pR_unique_marker_beta'",
      "expected_output_description": "Both unique marker strings appear in the formatted output",
      "expected_error": "",
      "assertions": [
        "A unique random-like string placed in system_description appears verbatim in the output",
        "A unique random-like string placed in primary_goal appears verbatim in the output"
      ]
    },
    {
      "id": "goodhart_challenge_prompt_contains_model_data",
      "description": "Challenge prompts must include the actual problem model data, not just posture instructions with a static placeholder",
      "function": "_challenge_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with system_description='UNIQUE_SENTINEL_7x9q' tested with each Posture variant",
      "expected_output_description": "The sentinel string appears in every posture's challenge prompt",
      "expected_error": "",
      "assertions": [
        "A unique string in the problem model's system_description appears in the challenge prompt output",
        "This holds for all five posture variants"
      ]
    },
    {
      "id": "goodhart_understand_prompt_contains_model_data",
      "description": "Understand prompt must embed the actual current problem model state, not a static template",
      "function": "_understand_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with primary_goal='SENTINEL_understand_8k2m'",
      "expected_output_description": "The sentinel string from the model appears in the returned prompt",
      "expected_error": "",
      "assertions": [
        "A unique string in the problem model's primary_goal appears in the understand prompt",
        "A different model produces a different understand prompt"
      ]
    },
    {
      "id": "goodhart_synthesize_prompt_contains_model_data",
      "description": "Synthesize prompt must embed the actual problem model state, not a static template",
      "function": "_synthesize_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with stakes='SENTINEL_synth_4j7n'",
      "expected_output_description": "The sentinel string appears in the returned synthesize prompt",
      "expected_error": "",
      "assertions": [
        "A unique string in the problem model's stakes field appears in the synthesize prompt"
      ]
    },
    {
      "id": "goodhart_get_system_prompt_dispatches_correctly",
      "description": "get_system_prompt must dispatch to different builders per phase, producing structurally different prompts",
      "function": "get_system_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Same ProblemModel with all three phases (CHALLENGE uses SKEPTIC posture)",
      "expected_output_description": "All three prompts are distinct strings",
      "expected_error": "",
      "assertions": [
        "UNDERSTAND prompt differs from CHALLENGE prompt for same model",
        "UNDERSTAND prompt differs from SYNTHESIZE prompt for same model",
        "CHALLENGE prompt differs from SYNTHESIZE prompt for same model"
      ]
    },
    {
      "id": "goodhart_revision_prompt_feedback_verbatim",
      "description": "The revision prompt must contain the feedback string exactly as given, not summarized or transformed",
      "function": "get_revision_prompt",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "feedback='Please change:\\n1. Add \"security\" section\\n2. Remove redundant constraints\\n{keep braces}'",
      "expected_output_description": "The exact feedback string including newlines, quotes, and braces appears in the output",
      "expected_error": "",
      "assertions": [
        "A multi-line feedback string with special chars appears verbatim in the output",
        "Feedback with embedded newlines and quotes is preserved exactly"
      ]
    },
    {
      "id": "goodhart_revision_prompt_contains_novel_model_data",
      "description": "The revision prompt must serialize the provided problem model, not use a cached or static representation",
      "function": "get_revision_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "feedback='fix it', ProblemModel with system_description='REVISION_SENTINEL_3m8k'",
      "expected_output_description": "The sentinel string appears in the output",
      "expected_error": "",
      "assertions": [
        "A unique sentinel in the model's system_description appears in the revision prompt output"
      ]
    },
    {
      "id": "goodhart_revision_prompt_regeneration_instruction",
      "description": "The revision prompt must instruct full regeneration of artifacts, not incremental patching",
      "function": "get_revision_prompt",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "feedback='Add more detail to constraints', default ProblemModel",
      "expected_output_description": "Output includes regeneration-oriented instruction language",
      "expected_error": "",
      "assertions": [
        "The output contains language indicating regeneration/recreation rather than patching/updating"
      ]
    },
    {
      "id": "goodhart_select_posture_env_mixed_case",
      "description": "select_posture should handle various mixed-case env var values, not just the specific cases in visible tests",
      "function": "select_posture",
      "category": "edge_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "override=None with various mixed-case CONSTRAIN_POSTURE env var values",
      "expected_output_description": "Each resolves to the correct Posture enum member",
      "expected_error": "",
      "assertions": [
        "'Adversarial' (title case) resolves to ADVERSARIAL posture",
        "'sKePtIc' (random case) resolves to SKEPTIC posture",
        "'collaborator' (lower case) resolves to COLLABORATOR posture"
      ]
    },
    {
      "id": "goodhart_select_posture_env_empty_string",
      "description": "An empty CONSTRAIN_POSTURE env var should behave as if unset (fall through to random) or raise invalid_env_posture error",
      "function": "select_posture",
      "category": "edge_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "override=None, CONSTRAIN_POSTURE env var set to empty string",
      "expected_output_description": "Either a valid Posture is returned (random fallthrough) or an appropriate error is raised",
      "expected_error": "",
      "assertions": [
        "With CONSTRAIN_POSTURE='' and override=None, either raises an error or falls through to random selection returning a valid Posture"
      ]
    },
    {
      "id": "goodhart_select_posture_random_covers_all",
      "description": "When falling through to random selection, all five postures must be possible outcomes (not hardcoded to one)",
      "function": "select_posture",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "override=None, CONSTRAIN_POSTURE unset, called 100 times",
      "expected_output_description": "Multiple distinct Posture values are returned across calls",
      "expected_error": "",
      "assertions": [
        "Over many calls with no override and no env var, at least 3 distinct postures are observed (probabilistic but near-certain with enough trials)"
      ]
    },
    {
      "id": "goodhart_select_posture_override_all_variants",
      "description": "The override parameter must work for all five posture variants, not just the one tested in visible tests",
      "function": "select_posture",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Each Posture variant passed as override with no env var set",
      "expected_output_description": "Each override value is returned unchanged",
      "expected_error": "",
      "assertions": [
        "Each of the 5 Posture enum members, when passed as override, is returned exactly"
      ]
    },
    {
      "id": "goodhart_get_posture_description_meaningful_content",
      "description": "Posture descriptions should contain meaningful content related to the posture's behavioral lens, not arbitrary placeholder text",
      "function": "get_posture_description",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Each Posture variant",
      "expected_output_description": "Each description semantically relates to its posture's documented behavior",
      "expected_error": "",
      "assertions": [
        "ADVERSARIAL description contains words related to breaking/attacking/exploiting",
        "CONTRARIAN description contains words related to opposing/contrary/opposite",
        "CRITIC description contains words related to quality/completeness/rigor",
        "SKEPTIC description contains words related to evidence/questioning/doubt",
        "COLLABORATOR description contains words related to constructive/gaps/alternatives"
      ]
    },
    {
      "id": "goodhart_challenge_json_block_present_all_postures",
      "description": "The JSON block instruction must be present in challenge prompts for ALL posture variants, not just the ones visible tests check",
      "function": "_challenge_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Default ProblemModel with each of the 5 Posture variants",
      "expected_output_description": "All 5 challenge prompts contain JSON block instruction content",
      "expected_error": "",
      "assertions": [
        "Every posture variant's challenge prompt contains JSON-related output formatting instruction"
      ]
    },
    {
      "id": "goodhart_synthesize_both_artifacts_mentioned",
      "description": "Synthesize prompt must mention both output artifacts with clear delimiters, not just one",
      "function": "_synthesize_prompt",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Default ProblemModel",
      "expected_output_description": "Both artifact names and delimiter instructions present",
      "expected_error": "",
      "assertions": [
        "Output contains 'prompt.md' reference",
        "Output contains 'constraints.yaml' reference",
        "Output contains delimiter or separator instructions for the two artifacts"
      ]
    },
    {
      "id": "goodhart_revision_prompt_both_artifacts_mentioned",
      "description": "Revision prompt must specify both prompt.md and constraints.yaml output formats",
      "function": "get_revision_prompt",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "feedback='improve clarity', default ProblemModel",
      "expected_output_description": "Both artifact format specifications are present",
      "expected_error": "",
      "assertions": [
        "Output contains 'prompt.md' reference",
        "Output contains 'constraints.yaml' reference"
      ]
    },
    {
      "id": "goodhart_format_model_challenged_assumptions_and_risks",
      "description": "The format function must handle challenged_assumptions and risk_areas list fields, not just the basic fields",
      "function": "_format_problem_model",
      "category": "edge_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with challenged_assumptions=['Users always have fast internet - CHALLENGED: many users on 3G'] and risk_areas=['Single point of failure in auth service']",
      "expected_output_description": "Both challenged assumptions and risk areas appear in formatted output",
      "expected_error": "",
      "assertions": [
        "A model with challenged_assumptions populated includes those values in output",
        "A model with risk_areas populated includes those values in output"
      ]
    },
    {
      "id": "goodhart_get_system_prompt_challenge_each_posture",
      "description": "get_system_prompt with CHALLENGE phase must work correctly for ALL posture variants, producing posture-specific content",
      "function": "get_system_prompt",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Phase.CHALLENGE with same ProblemModel and each Posture variant",
      "expected_output_description": "5 distinct non-empty prompts with posture-specific instructions",
      "expected_error": "",
      "assertions": [
        "Calling with each of the 5 Posture variants returns a non-empty string",
        "Each result contains posture-specific content",
        "All 5 results are distinct from each other"
      ]
    },
    {
      "id": "goodhart_format_model_long_lists",
      "description": "The format function should handle lists with many items, not just the 2-3 items likely used in visible tests",
      "function": "_format_problem_model",
      "category": "edge_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with dependencies=[f'dep_{i}' for i in range(10)]",
      "expected_output_description": "All 10 dependency items appear in the formatted output",
      "expected_error": "",
      "assertions": [
        "A model with 10 dependencies renders all 10 in the output",
        "Each item text appears in the output"
      ]
    },
    {
      "id": "goodhart_revision_prompt_tab_only_feedback_rejected",
      "description": "Feedback consisting only of tabs or other whitespace characters should be rejected as empty",
      "function": "get_revision_prompt",
      "category": "error_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "feedback='\\t\\t\\t', default ProblemModel",
      "expected_output_description": "",
      "expected_error": "empty_feedback",
      "assertions": [
        "Feedback of '\\t\\t\\t' raises an error"
      ]
    },
    {
      "id": "goodhart_select_posture_env_with_whitespace",
      "description": "CONSTRAIN_POSTURE env var with leading/trailing whitespace around a valid name should either work or raise a clear error",
      "function": "select_posture",
      "category": "edge_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "override=None, CONSTRAIN_POSTURE=' ADVERSARIAL ' (with spaces)",
      "expected_output_description": "Either valid posture returned or appropriate error raised",
      "expected_error": "",
      "assertions": [
        "With CONSTRAIN_POSTURE=' ADVERSARIAL ' and override=None, either returns ADVERSARIAL or raises invalid_env_posture"
      ]
    },
    {
      "id": "goodhart_understand_prompt_varies_with_model",
      "description": "The understand prompt must change when the problem model changes, proving it actually embeds the model",
      "function": "_understand_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Two different ProblemModel instances with different system_description values",
      "expected_output_description": "Two distinct prompts, each containing its model's data",
      "expected_error": "",
      "assertions": [
        "Understand prompt with model A (system_description='AAA') differs from understand prompt with model B (system_description='BBB')",
        "Each prompt contains its respective model's data"
      ]
    },
    {
      "id": "goodhart_challenge_prompt_model_propagation",
      "description": "Challenge prompt must change when the problem model changes, for every posture",
      "function": "_challenge_prompt",
      "category": "invariant",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Two ProblemModels with different system_description, tested with CRITIC posture",
      "expected_output_description": "Distinct prompts containing their respective model data",
      "expected_error": "",
      "assertions": [
        "For at least one posture, challenge prompt with model A differs from challenge prompt with model B",
        "Each prompt contains its respective model's unique content"
      ]
    },
    {
      "id": "goodhart_synthesize_yaml_schema_spec",
      "description": "Synthesize prompt must include YAML schema specification details, not just mention the word 'yaml'",
      "function": "_synthesize_prompt",
      "category": "happy_path",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "Default ProblemModel",
      "expected_output_description": "YAML schema specification with structural details is present",
      "expected_error": "",
      "assertions": [
        "Output contains structural YAML-related keywords like 'yaml' or 'YAML'",
        "Output contains specification of what the YAML structure should contain"
      ]
    },
    {
      "id": "goodhart_format_model_open_questions",
      "description": "The format function must properly render the open_questions list field when populated",
      "function": "_format_problem_model",
      "category": "edge_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "ProblemModel with only open_questions populated",
      "expected_output_description": "Both question strings appear in formatted output with appropriate labeling",
      "expected_error": "",
      "assertions": [
        "A model with open_questions=['How will auth scale?', 'What is the SLA target?'] includes both questions in the output"
      ]
    },
    {
      "id": "goodhart_select_posture_env_partial_match_rejected",
      "description": "CONSTRAIN_POSTURE env var with a partial posture name should be rejected, not matched as prefix",
      "function": "select_posture",
      "category": "error_case",
      "setup_description": "",
      "mock_dependencies": {},
      "input_values": {},
      "input_description": "override=None, CONSTRAIN_POSTURE set to partial posture names",
      "expected_output_description": "",
      "expected_error": "invalid_env_posture",
      "assertions": [
        "'ADVER' as env var value raises invalid_env_posture error",
        "'COLL' as env var value raises invalid_env_posture error"
      ]
    }
  ],
  "test_language": "python",
  "generated_code": "\"\"\"\nHidden adversarial acceptance tests for posture.py (Posture & Prompt Generation component).\n\nThese tests target gaps in visible test coverage to catch implementations that\nhardcode returns, skip validation, or only handle specific visible test inputs.\n\"\"\"\nimport os\nimport pytest\nfrom unittest.mock import patch\n\nfrom src.models import Phase, Posture, ProblemModel\nfrom src.posture import (\n    get_system_prompt,\n    get_revision_prompt,\n    select_posture,\n    get_posture_description,\n    _format_problem_model,\n    _understand_prompt,\n    _challenge_prompt,\n    _synthesize_prompt,\n    POSTURE_DESCRIPTIONS,\n)\n\n\ndef _make_model(**kwargs) -> ProblemModel:\n    \"\"\"Helper to create ProblemModel with given overrides.\"\"\"\n    return ProblemModel(**kwargs)\n\n\n# ---------------------------------------------------------------------------\n# _format_problem_model: field omission, bullet rendering, novel content\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartFormatProblemModel:\n\n    def test_goodhart_format_model_omits_default_fields(self):\n        \"\"\"Fields at their default (empty) value should be omitted from output.\"\"\"\n        model_desc_only = _make_model(system_description=\"My web service\")\n        model_goal_only = _make_model(primary_goal=\"Improve latency\")\n        \n        out_desc = _format_problem_model(model_desc_only)\n        out_goal = _format_problem_model(model_goal_only)\n        \n        # They should differ since they have different fields populated\n        assert out_desc != out_goal, \"Format output should differ for models with different populated fields\"\n        \n        # The desc-only output should not mention goal-related labels when goal is empty\n        assert \"My web service\" in out_desc\n        assert \"Improve latency\" in out_goal\n        assert \"Improve latency\" not in out_desc\n        assert \"My web service\" not in out_goal\n\n    def test_goodhart_format_model_list_bullets(self):\n        \"\"\"List fields should be rendered as bulleted sub-items, each on its own line.\"\"\"\n        model = _make_model(dependencies=[\"Redis cache\", \"PostgreSQL 15\", \"RabbitMQ\"])\n        out = _format_problem_model(model)\n        \n        # Each item should appear in the output\n        assert \"Redis cache\" in out\n        assert \"PostgreSQL 15\" in out\n        assert \"RabbitMQ\" in out\n        \n        # Items should be on separate lines (bulleted)\n        lines = out.split(\"\\n\")\n        redis_lines = [l for l in lines if \"Redis cache\" in l]\n        pg_lines = [l for l in lines if \"PostgreSQL 15\" in l]\n        assert len(redis_lines) >= 1\n        assert len(pg_lines) >= 1\n        # They should be on different lines\n        assert redis_lines[0] != pg_lines[0]\n\n    def test_goodhart_format_model_novel_content_passthrough(self):\n        \"\"\"Format must actually serialize field values, not return pre-baked content.\"\"\"\n        model = _make_model(\n            system_description=\"xK9mQ_unique_marker_alpha\",\n            primary_goal=\"zW3pR_unique_marker_beta\",\n        )\n        out = _format_problem_model(model)\n        assert \"xK9mQ_unique_marker_alpha\" in out\n        assert \"zW3pR_unique_marker_beta\" in out\n\n    def test_goodhart_format_model_challenged_assumptions_and_risks(self):\n        \"\"\"Format must handle challenged_assumptions and risk_areas fields.\"\"\"\n        model = _make_model(\n            challenged_assumptions=[\"Users always have fast internet - CHALLENGED: many on 3G\"],\n            risk_areas=[\"Single point of failure in auth service\"],\n        )\n        out = _format_problem_model(model)\n        assert \"Users always have fast internet\" in out\n        assert \"Single point of failure in auth service\" in out\n\n    def test_goodhart_format_model_open_questions(self):\n        \"\"\"Format must render open_questions list field properly.\"\"\"\n        model = _make_model(\n            open_questions=[\"How will auth scale?\", \"What is the SLA target?\"]\n        )\n        out = _format_problem_model(model)\n        assert \"How will auth scale?\" in out\n        assert \"What is the SLA target?\" in out\n\n    def test_goodhart_format_model_long_lists(self):\n        \"\"\"Format should handle lists with many items, rendering all of them.\"\"\"\n        deps = [f\"dep_{i}\" for i in range(10)]\n        model = _make_model(dependencies=deps)\n        out = _format_problem_model(model)\n        for dep in deps:\n            assert dep in out, f\"Expected '{dep}' in formatted output\"\n\n\n# ---------------------------------------------------------------------------\n# _understand_prompt: model embedding, variability\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartUnderstandPrompt:\n\n    def test_goodhart_understand_prompt_contains_model_data(self):\n        \"\"\"Understand prompt must embed actual model state, not a static template.\"\"\"\n        model = _make_model(primary_goal=\"SENTINEL_understand_8k2m\")\n        out = _understand_prompt(model)\n        assert \"SENTINEL_understand_8k2m\" in out\n\n    def test_goodhart_understand_prompt_varies_with_model(self):\n        \"\"\"Understand prompt must change when problem model changes.\"\"\"\n        model_a = _make_model(system_description=\"AAA_system_unique\")\n        model_b = _make_model(system_description=\"BBB_system_unique\")\n        out_a = _understand_prompt(model_a)\n        out_b = _understand_prompt(model_b)\n        assert out_a != out_b, \"Understand prompt should differ for different models\"\n        assert \"AAA_system_unique\" in out_a\n        assert \"BBB_system_unique\" in out_b\n\n\n# ---------------------------------------------------------------------------\n# _challenge_prompt: model embedding, JSON block in all postures\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartChallengePrompt:\n\n    def test_goodhart_challenge_prompt_contains_model_data(self):\n        \"\"\"Challenge prompts must include actual problem model data for all postures.\"\"\"\n        model = _make_model(system_description=\"UNIQUE_SENTINEL_7x9q\")\n        for posture in Posture:\n            out = _challenge_prompt(model, posture)\n            assert \"UNIQUE_SENTINEL_7x9q\" in out, (\n                f\"Challenge prompt for {posture.name} should contain model data\"\n            )\n\n    def test_goodhart_challenge_json_block_present_all_postures(self):\n        \"\"\"JSON block instruction must be present in challenge prompts for ALL posture variants.\"\"\"\n        model = _make_model()\n        # Get a reference JSON block indicator from UNDERSTAND (which we know has it)\n        understand_out = _understand_prompt(model)\n        # Find a distinctive JSON-related substring that should be the shared _JSON_BLOCK_INSTRUCTION\n        # We check for common indicators of JSON block instruction\n        for posture in Posture:\n            out = _challenge_prompt(model, posture)\n            # Should contain JSON-related formatting instruction\n            json_indicators = [\"json\", \"JSON\", \"```\"]\n            has_json = any(ind.lower() in out.lower() for ind in json_indicators)\n            assert has_json, (\n                f\"Challenge prompt for {posture.name} should contain JSON block instruction\"\n            )\n\n    def test_goodhart_challenge_prompt_model_propagation(self):\n        \"\"\"Challenge prompt must change when problem model changes.\"\"\"\n        model_a = _make_model(system_description=\"MODEL_A_content_unique_789\")\n        model_b = _make_model(system_description=\"MODEL_B_content_unique_321\")\n        out_a = _challenge_prompt(model_a, Posture.CRITIC)\n        out_b = _challenge_prompt(model_b, Posture.CRITIC)\n        assert out_a != out_b\n        assert \"MODEL_A_content_unique_789\" in out_a\n        assert \"MODEL_B_content_unique_321\" in out_b\n\n\n# ---------------------------------------------------------------------------\n# _synthesize_prompt: model embedding, artifact specs, YAML schema\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartSynthesizePrompt:\n\n    def test_goodhart_synthesize_prompt_contains_model_data(self):\n        \"\"\"Synthesize prompt must embed actual problem model state.\"\"\"\n        model = _make_model(stakes=\"SENTINEL_synth_4j7n\")\n        out = _synthesize_prompt(model)\n        assert \"SENTINEL_synth_4j7n\" in out\n\n    def test_goodhart_synthesize_both_artifacts_mentioned(self):\n        \"\"\"Synthesize prompt must mention both output artifacts with delimiters.\"\"\"\n        model = _make_model()\n        out = _synthesize_prompt(model)\n        out_lower = out.lower()\n        assert \"prompt.md\" in out_lower or \"prompt.md\" in out\n        assert \"constraints.yaml\" in out_lower or \"constraints.yaml\" in out\n\n    def test_goodhart_synthesize_yaml_schema_spec(self):\n        \"\"\"Synthesize prompt must include YAML schema specification details.\"\"\"\n        model = _make_model()\n        out = _synthesize_prompt(model)\n        out_lower = out.lower()\n        assert \"yaml\" in out_lower, \"Synthesize prompt should contain YAML specification\"\n\n\n# ---------------------------------------------------------------------------\n# get_system_prompt: dispatch correctness, all postures\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartGetSystemPrompt:\n\n    def test_goodhart_get_system_prompt_dispatches_correctly(self):\n        \"\"\"get_system_prompt must produce structurally different prompts per phase.\"\"\"\n        model = _make_model(system_description=\"dispatch_test_content\")\n        \n        out_understand = get_system_prompt(Phase.UNDERSTAND, model, None)\n        out_challenge = get_system_prompt(Phase.CHALLENGE, model, Posture.SKEPTIC)\n        out_synthesize = get_system_prompt(Phase.SYNTHESIZE, model, None)\n        \n        assert out_understand != out_challenge\n        assert out_understand != out_synthesize\n        assert out_challenge != out_synthesize\n\n    def test_goodhart_get_system_prompt_challenge_each_posture(self):\n        \"\"\"get_system_prompt with CHALLENGE must work for ALL posture variants with distinct content.\"\"\"\n        model = _make_model(system_description=\"posture_variant_test\")\n        results = {}\n        for posture in Posture:\n            result = get_system_prompt(Phase.CHALLENGE, model, posture)\n            assert result, f\"Challenge prompt for {posture.name} should be non-empty\"\n            assert \"posture_variant_test\" in result, (\n                f\"Challenge prompt for {posture.name} should contain model data\"\n            )\n            results[posture] = result\n        \n        # All 5 should be pairwise distinct\n        values = list(results.values())\n        for i in range(len(values)):\n            for j in range(i + 1, len(values)):\n                assert values[i] != values[j], (\n                    f\"Challenge prompts for different postures should be distinct\"\n                )\n\n\n# ---------------------------------------------------------------------------\n# get_revision_prompt: feedback verbatim, model embedding, regeneration\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartGetRevisionPrompt:\n\n    def test_goodhart_revision_prompt_feedback_verbatim(self):\n        \"\"\"Revision prompt must contain the feedback string exactly as given.\"\"\"\n        feedback = 'Please change:\\n1. Add \"security\" section\\n2. Remove redundant constraints\\n{keep braces}'\n        model = _make_model()\n        out = get_revision_prompt(feedback, model)\n        assert feedback in out, \"Feedback must appear verbatim in revision prompt\"\n\n    def test_goodhart_revision_prompt_contains_novel_model_data(self):\n        \"\"\"Revision prompt must serialize the provided problem model, not a static representation.\"\"\"\n        model = _make_model(system_description=\"REVISION_SENTINEL_3m8k\")\n        out = get_revision_prompt(\"fix it\", model)\n        assert \"REVISION_SENTINEL_3m8k\" in out\n\n    def test_goodhart_revision_prompt_regeneration_instruction(self):\n        \"\"\"Revision prompt must instruct full regeneration, not incremental patching.\"\"\"\n        model = _make_model()\n        out = get_revision_prompt(\"Add more detail to constraints\", model)\n        out_lower = out.lower()\n        # Should contain regeneration-oriented language\n        regen_terms = [\"regenerat\", \"recreat\", \"generat\", \"produce\", \"create\", \"write\"]\n        patch_only = all(term not in out_lower for term in regen_terms)\n        assert not patch_only, (\n            \"Revision prompt should contain regeneration-oriented language\"\n        )\n\n    def test_goodhart_revision_prompt_both_artifacts_mentioned(self):\n        \"\"\"Revision prompt must specify both prompt.md and constraints.yaml.\"\"\"\n        model = _make_model()\n        out = get_revision_prompt(\"improve clarity\", model)\n        assert \"prompt.md\" in out or \"prompt.md\" in out.lower()\n        assert \"constraints.yaml\" in out or \"constraints.yaml\" in out.lower()\n\n    def test_goodhart_revision_prompt_tab_only_feedback_rejected(self):\n        \"\"\"Feedback consisting only of tab characters should be rejected as empty.\"\"\"\n        model = _make_model()\n        with pytest.raises(Exception):\n            get_revision_prompt(\"\\t\\t\\t\", model)\n\n\n# ---------------------------------------------------------------------------\n# select_posture: comprehensive coverage\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartSelectPosture:\n\n    def test_goodhart_select_posture_env_mixed_case_variants(self):\n        \"\"\"select_posture should handle various mixed-case env var values.\"\"\"\n        test_cases = [\n            (\"Adversarial\", Posture.ADVERSARIAL),\n            (\"sKePtIc\", Posture.SKEPTIC),\n            (\"collaborator\", Posture.COLLABORATOR),\n            (\"CONTRARIAN\", Posture.CONTRARIAN),\n            (\"Critic\", Posture.CRITIC),\n        ]\n        for env_val, expected in test_cases:\n            with patch.dict(os.environ, {\"CONSTRAIN_POSTURE\": env_val}):\n                result = select_posture(None)\n                assert result == expected, (\n                    f\"CONSTRAIN_POSTURE='{env_val}' should resolve to {expected.name}\"\n                )\n\n    def test_goodhart_select_posture_random_covers_multiple(self):\n        \"\"\"Random selection must be able to produce multiple distinct postures.\"\"\"\n        with patch.dict(os.environ, {}, clear=False):\n            # Ensure CONSTRAIN_POSTURE is not set\n            env = os.environ.copy()\n            env.pop(\"CONSTRAIN_POSTURE\", None)\n            with patch.dict(os.environ, env, clear=True):\n                results = set()\n                for _ in range(200):\n                    result = select_posture(None)\n                    assert isinstance(result, Posture)\n                    results.add(result)\n                assert len(results) >= 2, (\n                    \"Random posture selection should produce multiple distinct values over many calls\"\n                )\n\n    def test_goodhart_select_posture_override_all_variants(self):\n        \"\"\"Override must work for ALL five posture variants.\"\"\"\n        for posture in Posture:\n            result = select_posture(posture)\n            assert result == posture, (\n                f\"Override with {posture.name} should return {posture.name}\"\n            )\n\n    def test_goodhart_select_posture_env_partial_match_rejected(self):\n        \"\"\"Partial posture names in env var should be rejected.\"\"\"\n        for partial in [\"ADVER\", \"COLL\", \"CRIT\", \"SKEP\", \"CONTRA\"]:\n            with patch.dict(os.environ, {\"CONSTRAIN_POSTURE\": partial}):\n                with pytest.raises(Exception):\n                    select_posture(None)\n\n    def test_goodhart_select_posture_env_empty_string(self):\n        \"\"\"Empty CONSTRAIN_POSTURE env var should either raise or fall through.\"\"\"\n        with patch.dict(os.environ, {\"CONSTRAIN_POSTURE\": \"\"}):\n            try:\n                result = select_posture(None)\n                # If it doesn't raise, it should return a valid Posture (fell through)\n                assert isinstance(result, Posture)\n            except Exception:\n                # Raising an error is also acceptable behavior\n                pass\n\n    def test_goodhart_select_posture_env_with_whitespace(self):\n        \"\"\"CONSTRAIN_POSTURE with leading/trailing whitespace should either work or raise clear error.\"\"\"\n        with patch.dict(os.environ, {\"CONSTRAIN_POSTURE\": \" ADVERSARIAL \"}):\n            try:\n                result = select_posture(None)\n                # If it works, should resolve to ADVERSARIAL\n                assert result == Posture.ADVERSARIAL\n            except Exception:\n                # Raising invalid_env_posture is also acceptable\n                pass\n\n\n# ---------------------------------------------------------------------------\n# get_posture_description: semantic content\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartGetPostureDescription:\n\n    def test_goodhart_posture_description_meaningful_content(self):\n        \"\"\"Posture descriptions should semantically relate to the posture's documented behavior.\"\"\"\n        desc_adv = get_posture_description(Posture.ADVERSARIAL).lower()\n        desc_con = get_posture_description(Posture.CONTRARIAN).lower()\n        desc_cri = get_posture_description(Posture.CRITIC).lower()\n        desc_ske = get_posture_description(Posture.SKEPTIC).lower()\n        desc_col = get_posture_description(Posture.COLLABORATOR).lower()\n        \n        # Each description should contain at least one semantically relevant term\n        adv_terms = [\"break\", \"attack\", \"exploit\", \"vulnerabilit\", \"weak\", \"fail\", \"flaw\", \"adversar\"]\n        assert any(t in desc_adv for t in adv_terms), (\n            f\"ADVERSARIAL description should relate to breaking/attacking: '{desc_adv}'\"\n        )\n        \n        con_terms = [\"opposite\", \"contrar\", \"against\", \"oppos\", \"argu\", \"reverse\", \"counter\", \"other side\"]\n        assert any(t in desc_con for t in con_terms), (\n            f\"CONTRARIAN description should relate to opposing: '{desc_con}'\"\n        )\n        \n        cri_terms = [\"quality\", \"complete\", \"rigor\", \"evaluat\", \"standard\", \"thorough\", \"critic\", \"assess\"]\n        assert any(t in desc_cri for t in cri_terms), (\n            f\"CRITIC description should relate to quality/rigor: '{desc_cri}'\"\n        )\n        \n        ske_terms = [\"evidence\", \"question\", \"doubt\", \"certain\", \"proof\", \"skepti\", \"assum\", \"support\", \"claim\"]\n        assert any(t in desc_ske for t in ske_terms), (\n            f\"SKEPTIC description should relate to evidence/questioning: '{desc_ske}'\"\n        )\n        \n        col_terms = [\"constructi\", \"gap\", \"alternativ\", \"collaborat\", \"suggest\", \"improv\", \"together\", \"help\", \"build\"]\n        assert any(t in desc_col for t in col_terms), (\n            f\"COLLABORATOR description should relate to constructive approach: '{desc_col}'\"\n        )\n\n\n# ---------------------------------------------------------------------------\n# POSTURE_DESCRIPTIONS constant: key type check\n# ---------------------------------------------------------------------------\n\nclass TestGoodhartPostureDescriptions:\n\n    def test_goodhart_posture_descriptions_keys_are_enum_members(self):\n        \"\"\"POSTURE_DESCRIPTIONS keys must be actual Posture enum members, not strings.\"\"\"\n        for key in POSTURE_DESCRIPTIONS:\n            assert isinstance(key, Posture), (\n                f\"POSTURE_DESCRIPTIONS key {key!r} should be a Posture enum member\"\n            )\n\n    def test_goodhart_posture_descriptions_values_are_nonempty_strings(self):\n        \"\"\"All POSTURE_DESCRIPTIONS values must be non-empty strings.\"\"\"\n        for posture, desc in POSTURE_DESCRIPTIONS.items():\n            assert isinstance(desc, str), f\"Description for {posture} should be a string\"\n            assert len(desc.strip()) > 0, f\"Description for {posture} should be non-empty\"\n"
}